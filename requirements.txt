# ARGOS runtime dependencies.
# Pinned loosely (>=) for an early-stage project; tighten before any release.

# --- Core / models ---
pydantic>=2.6,<3

# --- API ---
fastapi>=0.110
uvicorn[standard]>=0.27

# --- Fingerprint DB (semantic embeddings — the differentiator) ---
# Pulls in torch as a transitive dependency. This is the heaviest install and
# what the demo needs. On first run it downloads a small (~80 MB) model.
sentence-transformers>=2.6

# --- Interaction analyzer (guardian model) ---
# Needed for the DeBERTa prompt-injection classifier. torch is required as a
# backend (installed with sentence-transformers above, but listed for clarity).
transformers>=4.40

# --- Dev / tests (kept here for simplicity in an early-stage repo) ---
pytest>=8.0
httpx>=0.27          # FastAPI TestClient
