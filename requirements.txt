# Dependencias de ejecución de ARGOS.
# Fijadas de forma laxa (>=) por ser fase inicial; ajustar antes de un release.

# --- Núcleo / modelos ---
pydantic>=2.6,<3

# --- API ---
fastapi>=0.110
uvicorn[standard]>=0.27

# --- Base de huellas (embeddings semánticos — el diferenciador) ---
# Arrastra torch como dependencia transitiva. Es la instalación más pesada y lo
# que necesita la demo. La primera vez descarga un modelo pequeño (~80 MB).
sentence-transformers>=2.6

# --- Interaction analyzer (modelo guardián) ---
# Necesario para el clasificador DeBERTa de inyección de prompt. torch se requiere
# como backend (ya lo instala sentence-transformers, pero se lista por claridad).
transformers>=4.40

# --- Desarrollo / tests (aquí por simplicidad en fase inicial) ---
pytest>=8.0
httpx>=0.27          # TestClient de FastAPI
